/**
 * ### Addition
 * @param {a} a first argumengt
 * @param {b} b second argumengt
 * 
 * @returns (number) Addition of Number 'a' and 'b'
 */
const Add = (a, b) => {
    return a+b
}

/**
 * ## Subtraction
 * @param {a} a first argumengt
 * @param {b} b second argumengt
 * 
 * @returns (number) Addition of Number 'a' and 'b'
 */

const Sub = (a, b) => {
    return a-b
}

const mul = () => {
    Add();
}